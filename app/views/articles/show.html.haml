#menu
  = render partial: 'articlemenu', locals: {mode: 'show', article: @article}
  = render partial: 'appmenu'

%h1= link_to @blog.name, blog_path

= render partial: 'article', locals: {article: @article}
